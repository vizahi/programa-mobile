import { StyleSheet } from "react-native";

const estilos = StyleSheet.create({
    container: {
        flex:1,
        alignItems:"center",

    },
    header: {
        fontSize: 30,
        fontWeight:"bold",
        color:"brown",
        marginTop:25,
    },
    description:{
        fontSize: 20,
        color:"black",
    }
})

export default estilos
